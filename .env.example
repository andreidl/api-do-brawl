# Copie este arquivo para .env e preencha com os valores reais.
# O .env fica FORA do git (ver .gitignore). NUNCA commitar segredos.

# Token da API oficial do Brawl Stars (developer.brawlstars.com)
BRAWL_API_TOKEN=

# String de conexão do Postgres (Supabase/Neon) — só na Fase 2. Vazio = usa SQLite local.
DATABASE_URL=
